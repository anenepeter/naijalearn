/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'jyltaekdutgeneccvumq.supabase.co', // Add your Supabase storage hostname here
    ],
  },
};

module.exports = nextConfig;